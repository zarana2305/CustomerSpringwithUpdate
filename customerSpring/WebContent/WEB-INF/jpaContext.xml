<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:tx="http://www.springframework.org/schema/tx"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.2.xsd
		http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-3.2.xsd">


<!-- in this component scan, the scan is generic,all the packages will be included -->
	<context:annotation-config />
	
<!--******used to scan all the packages from the base package******-->

	<context:component-scan base-package="com.cg.customer"/>
	
	<!-- This bean will hold references to our database -->
	<bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
		<property name="driverClassName" value="oracle.jdbc.driver.OracleDriver" />
		<property name="url" value="jdbc:oracle:thin:@10.219.34.3:1521:orcl" />
		<property name="username" value="trg333" />
		<property name="password" value="training333" />
	</bean>
	
	<!-- Spring way of entity manager factory configuration -->
	
	<bean id="entityManagerFactory" class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean">
          <property name="dataSource" ref="dataSource"/>
          
<!-- ***********value has to be the package name of the bean class*********-->
          
          
          <property name="packagesToScan" value="com.cg.customer.model"/>
          <property name="persistenceProviderClass" value="org.hibernate.jpa.HibernatePersistenceProvider"/>
          <property name="jpaPropertyMap">
          
              <map>
    		  <!-- entry key="hibernate.show_sql" value="true"/> -->
                  <entry key="hibernate.dialect" value="org.hibernate.dialect.Oracle10gDialect"/>
                  <entry key="hibernate.hbm2ddl.auto" value="update"/>
              </map>
          </property>
      </bean>
      
	<!-- This bean will handle all transaction within an application -->
	<bean id="transactionManager" class="org.springframework.orm.jpa.JpaTransactionManager">
		<property name="entityManagerFactory" ref="entityManagerFactory"/>
	</bean>
	<!-- used to enable tags(annotations) -->
	<!-- This is required to handle transactions via Annotations like @Transactional -->
	<tx:annotation-driven transaction-manager="transactionManager"/>
	

</beans>